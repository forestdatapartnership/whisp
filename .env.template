# Earth Engine project name
# PROJECT="my-ee-project"

# Optional: delete the local Earth Engine credentials file after each pytest run,
# so you sign in again in the browser next time (off unless set to 1)
# WHISP_CLEAR_EE_CREDS=1
